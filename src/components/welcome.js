import React from 'react';

export default class Welcome extends React.Component {
  render() {
    return (
      <div className="welcome">
        Pick files from the right index to edit them with this app.
      </div>
    );
  }
}